/**
 * Barrel for order types used by features and app routes.
 * Canonical definitions live in `@/types/order`.
 */
export type {
  AdminOrderListItem,
  AdminOrderDetail,
  HistoryDetailsJson,
  HistoryItemJson,
  OrderDisplayData,
  OrderHistoryEntry,
  ReturnableItem,
  UserOrderListItem,
  UserOrderListItemLine,
  UserReturnableItem,
} from "@/types/order";
